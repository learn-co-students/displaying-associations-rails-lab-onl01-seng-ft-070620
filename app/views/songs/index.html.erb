<h1>Song Index</h1>
<ul>
 <% @songs.each do |s| %>
   <!-- <li><%= link_to s.artist.name, artist_path(s.artist) %> - <%= link_to s.title, song_path(s) %> </li> -->
   <li> 
       <a href="<%= song_path(s) %>">
        <%= s.artist_name %>
        -
        <%= s.title %></a>
    </li>
 <% end %>
</ul>